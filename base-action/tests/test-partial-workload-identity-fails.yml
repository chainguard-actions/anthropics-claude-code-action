name: Test partial workload identity fails gracefully
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        continue-on-error: true
        with:
          prompt: "Say hello"
          # Only providing federation_rule_id without organization_id — partial workload identity
          anthropic_federation_rule_id: "fdrl_fake_rule_id"
          # anthropic_organization_id is intentionally omitted

      - name: Assert action failed due to partial workload identity config
        env:
          OUTCOME: ${{ steps.action.outcome }}
          CONCLUSION: ${{ steps.action.outputs.conclusion }}
        run: |
          echo "outcome: $OUTCOME"
          echo "conclusion: $CONCLUSION"
          if [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed as expected when only one workload identity field is provided"
          elif [ "$OUTCOME" = "success" ]; then
            echo "FAIL: action should not succeed with partial workload identity configuration"
            exit 1
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
