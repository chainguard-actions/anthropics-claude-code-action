name: Test custom branch prefix configuration
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: workflow_dispatch
      GITHUB_REPOSITORY: test-owner/test-repo
      GITHUB_EVENT_PATH: /tmp/github-events/event.json
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install Bun to system PATH
        run: |
          curl -fsSL https://bun.sh/install | bash
          # Copy bun to a location that is always in PATH
          cp "$HOME/.bun/bin/bun" /usr/local/bin/bun
          bun --version

      - name: Create fake event payload
        run: |
          mkdir -p /tmp/github-events
          cat > /tmp/github-events/event.json <<'EOF'
          {
            "repository": {
              "name": "test-repo",
              "owner": {
                "login": "test-owner"
              }
            },
            "sender": {
              "login": "test-user"
            },
            "workflow": ".github/workflows/test.yml"
          }
          EOF

      - id: action
        uses: .
        continue-on-error: true
        with:
          github_token: ${{ github.token }}
          prompt: "Say hello"
          branch_prefix: "claude-"
          trigger_phrase: "@ai"
          use_sticky_comment: "false"
          path_to_bun_executable: /usr/local/bin/bun
          # Intentionally omitting anthropic_api_key - will fail at validation

      - name: Assert action accepted custom inputs and failed at validation
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "action outcome: $OUTCOME"
          # The action should fail: either at GitHub API call (checkHumanActor)
          # or at validateEnvironmentVariables (no API key)
          # This confirms custom inputs are accepted without crashing at input parsing
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected failure at validation, got: $OUTCOME"
            exit 1
          fi
          echo "PASS: action accepted custom inputs and failed at expected validation step"
